// Calculate a rolling hash.
const crypto = require("crypto");
const hash = crypto.createHash("md5");

// hash.update("one");
// console.log(hash.copy().digest("hex"));

// hash.update("two");
// console.log(hash.copy().digest("hex"));

// hash.update("three");
// console.log(hash.copy().digest("hex"));

// const hash_2 = crypto.createHash("md5");

// hash_2.update("two");
// console.log(hash_2.copy().digest("hex"));

// hash_2.update("three");
// console.log(hash_2.copy().digest("hex"));
